<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gzu.classdemo.dao.BookMapper">
    <select id="findAll" resultType="Book">
        SELECT * FROM book
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR author LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="findCategories" resultType="string">
        SELECT DISTINCT category FROM book ORDER BY category
    </select>

    <select id="findById" resultType="Book">
        SELECT * FROM book WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book(title, author, category, price, stock, description, cover_url)
        VALUES(#{title}, #{author}, #{category}, #{price}, #{stock}, #{description}, #{coverUrl})
    </insert>

    <update id="update">
        UPDATE book
        SET title = #{title}, author = #{author}, category = #{category}, price = #{price},
            stock = #{stock}, description = #{description}, cover_url = #{coverUrl}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM book WHERE id = #{id}
    </delete>

    <update id="decreaseStock">
        UPDATE book SET stock = stock - #{quantity}
        WHERE id = #{id} AND stock >= #{quantity}
    </update>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM book
    </select>

    <select id="findLowStock" resultType="Book">
        SELECT * FROM book ORDER BY stock ASC LIMIT #{limit}
    </select>
</mapper>
