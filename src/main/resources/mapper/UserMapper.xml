<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gzu.classdemo.dao.UserMapper">
    <select id="findByUsername" resultType="User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findAll" resultType="User">
        SELECT * FROM user ORDER BY id
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, real_name, email, role, status)
        VALUES(#{username}, #{password}, #{realName}, #{email}, #{role}, #{status})
    </insert>

    <update id="update">
        UPDATE user
        SET real_name = #{realName}, email = #{email}, role = #{role}, status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
